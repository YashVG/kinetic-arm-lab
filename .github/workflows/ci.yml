name: Verify Kinetic

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: read

jobs:
  browser:
    name: Control tests and browser build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build

  calibration:
    name: Synthetic OpenCV calibration tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: tools/calibration/requirements.txt
      - run: python -m pip install -r tools/calibration/requirements.txt
      - run: python -m unittest discover -s tools/calibration
